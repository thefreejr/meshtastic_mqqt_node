# Анализ механизма статуса доставки сообщений в Meshtastic

## 📋 Краткое резюме

**Проблема:** В прямых сообщениях не всегда проставляется статус доставки "доставка подтверждена" (RECEIVED).

**Причина:** Android клиент устанавливает статус "доставка подтверждена" только если `fromId == p?.data?.to`, где:
- `fromId` = `packet.from` из ACK пакета (отправитель ACK = получатель исходного пакета)
- `p?.data?.to` = `to` исходного пакета (получатель исходного пакета)

**Решение:** Убедиться, что ACK пакет имеет правильное поле `from` (должно быть равно `to` исходного пакета).

---

## Как работает статус доставки в Android клиенте

### Логика определения статуса

**Android (MeshService.kt:1107-1126):**
```kotlin
private fun handleAckNak(requestId: Int, fromId: String, routingError: Int, relayNode: Int? = null) {
    val isAck = routingError == MeshProtos.Routing.Error.NONE_VALUE
    val p = packetRepository.get().getPacketById(requestId)
    // distinguish real ACKs coming from the intended receiver
    val m =
        when {
            isAck && fromId == p?.data?.to -> MessageStatus.RECEIVED  // "доставка подтверждена"
            isAck -> MessageStatus.DELIVERED  // "доставлено"
            else -> MessageStatus.ERROR
        }
    if (p != null && p.data.status != MessageStatus.RECEIVED) {
        p.data.status = m
        // ...
    }
}
```

**Ключевой момент:**
- `fromId` = `toNodeID(packet.from)` из ACK пакета (строка вида "!51A24D8E")
- `p?.data?.to` = `toNodeID(packet.to)` из исходного пакета (строка вида "!51A24D8E")
- Статус "доставка подтверждена" (RECEIVED) устанавливается только если `fromId == p?.data?.to`

**Это означает:**
- Для прямых сообщений: ACK должен иметь `from = to` исходного пакета
- Если ACK приходит от промежуточного узла (relay), статус будет "доставлено" (DELIVERED), а не "доставка подтверждена" (RECEIVED)

---

## Как это работает в firmware

### Создание ACK пакета

**Firmware (ReliableRouter.cpp:110, RoutingModule.cpp:50-58):**
```cpp
// В firmware используется getFrom(p) для определения адреса получателя ACK
sendAckNak(meshtastic_Routing_Error_NONE, getFrom(p), p->id, p->channel, ...);

// getFrom возвращает наш node_num для пакетов с from=0
NodeNum getFrom(const meshtastic_MeshPacket *p)
{
    return (p->from == 0) ? nodeDB->getNodeNum() : p->from;
}
```

**Логика:**
- Для пакетов от клиента (`from=0`): `getFrom(p)` возвращает наш `node_num`
- Для пакетов из mesh (`from=node_num`): `getFrom(p)` возвращает `node_num` отправителя
- ACK создается с `to = getFrom(p)`, `from = наш node_num`

### Отправка ACK

**Firmware (RoutingModule.cpp:50-58):**
```cpp
void RoutingModule::sendAckNak(meshtastic_Routing_Error err, NodeNum to, PacketId idFrom, ChannelIndex chIndex, uint8_t hopLimit, bool ackWantsAck)
{
    auto p = allocAckNak(err, to, idFrom, chIndex, hopLimit);
    p->want_ack = ackWantsAck;
    router->sendLocal(p); // we sometimes send directly to the local node
}
```

**Для прямых сообщений через MQTT:**
- Исходный пакет: `from=node_num_A` (отправитель), `to=node_num_B` (получатель)
- ACK создается с `to=node_num_A` (отправитель), `from=node_num_B` (получатель)
- ACK отправляется через MQTT обратно отправителю
- Отправитель получает ACK с `from=node_num_B` (получатель)
- Android клиент проверяет: `fromId == p?.data?.to` → `node_num_B == node_num_B` ✅

---

## Реализация в проекте

### Текущая реализация

**Проект (packet_handler.py:148-175):**
```python
def create_ack_packet(original_packet: mesh_pb2.MeshPacket, 
                      our_node_num: int, 
                      channel_index: int,
                      error_reason: int = None,
                      ack_wants_ack: bool = False) -> mesh_pb2.MeshPacket:
    packet_from = getattr(original_packet, 'from', 0)
    ack_packet.to = packet_from  # to = отправитель исходного пакета
    setattr(ack_packet, 'from', our_node_num)  # from = получатель (наш node_num)
```

**Для прямых сообщений через MQTT:**
- Исходный пакет: `from=node_num_A`, `to=node_num_B`
- ACK создается с `to=node_num_A`, `from=node_num_B`
- Это правильно! ✅

**Для пакетов от клиента через TCP:**
- Исходный пакет: `from=0`, `to=node_num`
- После обработки: `from=node_num`, `to=node_num` (изменено перед MQTT)
- ACK создается с `to=node_num` (уже изменен!), `from=node_num`
- Это может быть проблемой, но ACK отправляется локально, так что `to` не критично

---

## Проблема

### Возможные причины

1. **ACK не отправляется:** Пакет не имеет `want_ack=true` или не адресован нам
2. **Неправильное поле `from` в ACK:** ACK имеет неправильное поле `from` (не равно `to` исходного пакета)
3. **ACK приходит от промежуточного узла:** ACK приходит от relay узла, а не от получателя
4. **Проблема с обработкой ACK:** Android клиент не обрабатывает ACK правильно

### Проверка

Нужно проверить:
1. Отправляется ли ACK для прямых сообщений с `want_ack=true`?
2. Правильно ли установлено поле `from` в ACK пакете?
3. Приходит ли ACK от получателя или от промежуточного узла?

---

## Решение

### Для прямых сообщений через MQTT

ACK должен иметь:
- `from = наш node_num` (получатель исходного пакета)
- `to = packet_from` (отправитель исходного пакета)

Это уже реализовано правильно в `create_ack_packet`.

### Для пакетов от клиента через TCP

ACK должен иметь:
- `from = наш node_num`
- `to = 0` (клиент, оригинальный `from` до изменения)

Но так как ACK отправляется локально через TCP, `to` не критично. Важно только `from`.

---

## Следующие шаги

1. ✅ Добавлено детальное логирование для диагностики
2. Проверить логи - отправляется ли ACK для прямых сообщений?
3. Проверить поле `from` в ACK пакетах - правильно ли оно установлено?
4. Проверить, приходит ли ACK от получателя или от промежуточного узла

## Выводы

### Текущая реализация

**Логика создания ACK:**
- `ack_packet.from = our_node_num` (получатель исходного пакета) ✅
- `ack_packet.to = packet_from` (отправитель исходного пакета) ✅

**Для прямых сообщений через MQTT:**
- Исходный пакет: `from=node_num_A`, `to=node_num_B`
- ACK: `from=node_num_B`, `to=node_num_A`
- Android клиент проверяет: `fromId == p?.data?.to` → `node_num_B == node_num_B` ✅

**Логика правильная!** Проблема может быть в другом:
1. ACK не отправляется (проверить логи)
2. ACK приходит от промежуточного узла (relay), а не от получателя
3. Пакет не имеет `want_ack=true`
4. Проблема на стороне Android клиента

### Добавленное логирование

Теперь в логах будет видно:
- `ack_from` - поле `from` в ACK пакете (должно быть равно `to` исходного пакета)
- `ack_to` - поле `to` в ACK пакете (должно быть равно `from` исходного пакета)
- `original_from` - поле `from` в исходном пакете
- `original_to` - поле `to` в исходном пакете
- `(will be RECEIVED)` или `(will be DELIVERED)` - предсказание статуса

Это поможет диагностировать проблему.

