# Meshtastic MQTT Node Simulator

Python скрипт для имитации работы ноды Meshtastic с MQTT (без радиомодулей).
Поддерживает подключение веб-клиента client.meshtastic.com по HTTPS.

## Возможности

- ✅ Подключение к MQTT брокеру Meshtastic
- ✅ HTTPS веб-сервер для подключения веб-клиента
- ✅ API endpoints `/api/v1/fromradio` и `/api/v1/toradio`
- ✅ Мост между веб-клиентом и MQTT
- ✅ Обработка protobuf сообщений с правильным framing
- ✅ Поддержка самоподписанных SSL сертификатов

## Установка

```bash
pip install -r requirements_meshtastic_node.txt
```

Или вручную:
```bash
pip install paho-mqtt flask flask-cors meshtastic protobuf cryptography
```

## Использование

### Базовое использование (HTTP)

```bash
python meshtastic_mqtt_node.py --mqtt-broker mqtt.meshtastic.org --mqtt-port 1883 --https-port 8080
```

### С HTTPS (самоподписанный сертификат)

```bash
python meshtastic_mqtt_node.py \
    --mqtt-broker mqtt.meshtastic.org \
    --mqtt-port 1883 \
    --https-port 443 \
    --generate-cert
```

### С собственными SSL сертификатами

```bash
python meshtastic_mqtt_node.py \
    --mqtt-broker mqtt.meshtastic.org \
    --mqtt-port 1883 \
    --https-port 443 \
    --cert-file cert.pem \
    --key-file key.pem
```

### С MQTT аутентификацией

```bash
python meshtastic_mqtt_node.py \
    --mqtt-broker mqtt.example.com \
    --mqtt-port 1883 \
    --mqtt-username user \
    --mqtt-password pass \
    --https-port 8080
```

### С TLS для MQTT

```bash
python meshtastic_mqtt_node.py \
    --mqtt-broker mqtt.meshtastic.org \
    --mqtt-port 8883 \
    --https-port 443 \
    --generate-cert
```

## Параметры

### MQTT параметры

- `--mqtt-broker`, `-b` - Адрес MQTT брокера (обязательно)
- `--mqtt-port`, `-p` - Порт MQTT (по умолчанию: 1883)
- `--mqtt-username`, `-u` - MQTT username (опционально)
- `--mqtt-password`, `-P` - MQTT password (опционально)
- `--mqtt-root-topic`, `-t` - Корневой MQTT топик (по умолчанию: `msh`)
- `--mqtt-channel-id` - ID канала для MQTT (по умолчанию: `admin`)
- `--node-id`, `-n` - Node ID в формате `!XXXXXXXX` (по умолчанию: автогенерация)

### Веб-сервер параметры

- `--https-port` - Порт веб-сервера (по умолчанию: 8080)
- `--cert-file` - Путь к SSL сертификату (для HTTPS)
- `--key-file` - Путь к SSL приватному ключу (для HTTPS)
- `--generate-cert` - Автоматически создать самоподписанный SSL сертификат

## API Endpoints

### GET `/api/v1/fromradio`

Получение сообщений от ноды (для веб-клиента).

Параметры:
- `?all=true` - получить все доступные сообщения

Возвращает:
- Protobuf сообщения с framing (START1=0x94, START2=0xC3, длина, payload)

### PUT `/api/v1/toradio`

Отправка сообщений ноде (от веб-клиента).

Принимает:
- Protobuf сообщения с framing

Возвращает:
- Echo тех же данных

### OPTIONS

Поддержка CORS для обоих endpoints.

## Подключение веб-клиента

Для подключения через client.meshtastic.com:

1. **Запустите скрипт с HTTPS на порту 443:**
   ```bash
   python meshtastic_mqtt_node.py \
       --mqtt-broker mqtt.meshtastic.org \
       --mqtt-port 1883 \
       --https-port 443 \
       --generate-cert
   ```

2. **Настройте DNS или hosts файл:**
   - Добавьте запись, указывающую `client.meshtastic.com` на ваш сервер
   - Или используйте IP адрес напрямую

3. **Откройте client.meshtastic.com в браузере:**
   - Браузер предупредит о самоподписанном сертификате (это нормально)
   - Примите сертификат и продолжайте

## Формат сообщений

### Protobuf Framing

Все сообщения используют протокол StreamAPI:
- **START1**: `0x94`
- **START2**: `0xC3`
- **Длина**: 16 бит (big-endian)
- **Payload**: Protobuf данные

### MQTT топики

Сообщения публикуются в топики:
```
msh/2/e/{channel_id}/{gateway_id}
```

Где:
- `msh` - корневой топик (можно изменить)
- `2/e` - версия протокола и тип (encrypted)
- `{channel_id}` - ID канала (по умолчанию: `admin`)
- `{gateway_id}` - Node ID шлюза (автогенерация)

### ServiceEnvelope

Каждое сообщение в MQTT содержит `ServiceEnvelope`:
```protobuf
ServiceEnvelope {
    packet: MeshPacket        // Пакет сообщения
    channel_id: "admin"       // ID канала
    gateway_id: "!XXXXXXXX"  // Node ID шлюза
}
```

## Пример вывода

```
======================================================================
Meshtastic MQTT Node Simulator
======================================================================
✓ Подключено к MQTT брокеру mqtt.meshtastic.org:1883
✓ Подписка на топик: msh/2/e/admin/+
✓ Подписка на все каналы: msh/2/e/+/+
✓ SSL сертификаты загружены: meshtastic_cert.pem, meshtastic_key.pem
🌐 Запуск веб-сервера на порту 443 (HTTPS)

✓ Сервер запущен
  Node ID: !12345678
  MQTT: mqtt.meshtastic.org:1883
  Web: https://localhost:443
  API: https://localhost:443/api/v1/

👂 Ожидание подключений...
   (Нажмите Ctrl+C для выхода)

📨 Получено MQTT сообщение: msh/2/e/admin/!87654321, размер: 234 байт
✓ Сообщение добавлено в очередь для веб-клиента
📤 Получено сообщение от веб-клиента, размер: 156 байт
✓ Сообщение опубликовано в MQTT: msh/2/e/admin/!12345678
```

## Архитектура

```
┌─────────────────┐         ┌──────────────┐         ┌─────────────┐
│  Web Client     │◄──────►│  Web Server  │◄──────►│  MQTT       │
│  (Browser)      │  HTTPS │  (Flask)      │  MQTT  │  Broker     │
└─────────────────┘         └──────────────┘         └─────────────┘
                                    │
                                    │
                          ┌─────────▼─────────┐
                          │  MQTT Bridge      │
                          │  - Subscribe      │
                          │  - Publish        │
                          │  - Queue          │
                          └───────────────────┘
```

## Troubleshooting

### Не могу подключиться к MQTT

- Проверьте доступность брокера
- Проверьте порт (1883 для обычного, 8883 для TLS)
- Проверьте username/password если требуется аутентификация
- Проверьте firewall настройки

### Веб-клиент не подключается

- Убедитесь, что веб-сервер запущен на нужном порту
- Для HTTPS на порту 443 нужны права root (или используйте другой порт)
- Проверьте SSL сертификаты
- Для client.meshtastic.com нужен корректный DNS/hosts

### Сообщения не передаются

- Проверьте, что MQTT подключен (должны быть сообщения о подписке)
- Проверьте логи на наличие ошибок
- Убедитесь, что канал существует на других нодах
- Проверьте, что другие ноды подписаны на ваш канал

### Ошибки protobuf

- Убедитесь, что установлен meshtastic-python
- Проверьте версию protobuf
- Убедитесь, что формат сообщений соответствует протоколу Meshtastic

## Ограничения

1. **Без радиомодулей**: Скрипт не имеет радиомодулей, только MQTT мост
2. **Упрощенная обработка**: Некоторые функции реальной ноды не реализованы
3. **Без полной криптографии**: PKI расшифровка требует дополнительной реализации
4. **Самоподписанные сертификаты**: Для production нужны валидные SSL сертификаты

## Лицензия

Скрипт предоставляется как есть, для использования с проектом Meshtastic.

